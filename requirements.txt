# ContextFusion: Infrared and Visible Image Fusion Requirements
# Core deep learning framework
torch>=2.0.0
torchvision>=0.15.0

# Essential scientific computing libraries
numpy>=1.21.0
opencv-python>=4.5.0

# Computer vision and image processing
kornia>=0.6.0

# Transformer models and utilities
timm>=0.9.0
einops>=0.6.0

# Data processing and utilities
h5py>=3.7.0
tqdm>=4.64.0

# Training utilities
tensorboard>=2.10.0

# GPU acceleration (optional but recommended for performance)
triton>=2.0.0

# Neighborhood Attention (for deformable attention mechanisms)
natten>=0.14.0

# SSIM metric computation (for losses module)
pytorch-ssim>=0.1.0

# Additional utilities that may be needed
scipy>=1.9.0
Pillow>=8.0.0
matplotlib>=3.5.0
